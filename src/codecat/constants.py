# src/codecat/constants.py

"""
Global constants used throughout the Codecat application.

Keeping constants in one place makes it easier to manage and update them
if needed, rather than hardcoding them in multiple locations.
"""

DEFAULT_CONFIG_FILENAME: str = ".codecat_config.json"
"""Default name for the configuration file Codecat looks for."""

DEFAULT_OUTPUT_FILENAME: str = "codecat_output.md"
"""Default name for the generated output file."""
